# 三模块完整技术栈清单

> 来源：飞书知识库文档《三模块完整技术栈清单》
> 最后更新：2026-08-28

---

## 1️⃣ ESP32-1732S019 — 主控 + 显示

| 技术维度 | 技术选型 | 说明 |
|---|---|---|
| 芯片架构 | ESP32-S3 双核 Xtensa LX7 @ 240MHz | 性能够用，自带WiFi |
| 开发框架 | Arduino-ESP32 Core（推荐）/ ESP-IDF | Arduino上手快，ESP-IDF更底层可控 |
| 显示驱动 | ST7789 SPI 接口，1.9寸 170×320 | 板载布线，无需额外接线 |
| UI框架 | ① LVGL v8+（推荐）— 任务卡片/进度条/仪表盘<br>② TFT_eSPI — 轻量绘图函数 | LVGL交互好但占~30KB RAM，TFT_eSPI更轻但体验差 |
| WiFi通信 | ① ESP-NOW — 工牌间点对点，无需路由器<br>② MQTT — 连服务器做数据汇聚<br>③ WebSocket — 老板端Web看板实时推送 | 三者可组合使用 |
| 本地发现 | mDNS | 局域网内工牌互相发现 |
| 开发语言 | C/C++（Arduino）/ C++（ESP-IDF） | — |

---

## 2️⃣ KY-029 双色LED — 状态指示

| 技术维度 | 技术选型 | 说明 |
|---|---|---|
| 封装类型 | 红绿双色 3mm LED，共阳/共阴 | 两个引脚分别控制红/绿 |
| 驱动方式 | GPIO 直驱 + ledcWrite 硬件PWM | ESP32 GPIO输出能力足够，无需额外驱动芯片 |
| PWM控制 | LEDC通道，频率~1000Hz | 实现呼吸灯/脉冲提示效果 |
| 限流电阻 | ≥220Ω（串联） | 防止过流烧毁LED和GPIO |
| 驱动电流 | ~5-10mA/色 | ESP32 GPIO安全范围 |
| 功能映射 | 🔴 红灯 = 异常/待处理<br>🟢 绿灯 = 正常/已完成<br>🟡 红+绿同亮 = 过渡/警示 | 软件逻辑控制颜色组合 |

---

## 3️⃣ KY-004 按键 — 交互确认

| 技术维度 | 技术选型 | 说明 |
|---|---|---|
| 按键类型 | 机械轻触按键 | 自带上拉电阻模块 |
| 消抖方案 | ① 硬件中断 attachInterrupt + millis() 延时去抖<br>② OneButton库（推荐）— 封装好的短按/长按/双击 | OneButton库大大简化开发 |
| 中断策略 | FALLING边沿触发 + 50ms消抖窗口 | 避免抖动误触发 |
| 交互映射 | 短按 = 流程确认/下一步<br>长按2s = 上报异常/红灯<br>双击 = 切换任务/撤回 | 软件层定义手势语义 |
| GPIO模式 | INPUT_PULLUP（内部上拉） | 按下为LOW，松开为HIGH |

---

## 🏗️ 整体软件技术栈一览

```
┌─────────────────────────────────────────┐
│           应用层 (您的业务逻辑)            │
│  任务看板 │ 品质反馈 │ 流程追踪 │ 工牌交互  │
├─────────────────────────────────────────┤
│         UI框架: LVGL v8+ / TFT_eSPI       │
├──────────────┬────────────────────────────┤
│  通信层       │  外设驱动层                  │
│  ESP-NOW     │  ST7789 显示驱动 (SPI)       │
│  MQTT        │  KY-029 PWM驱动 (LEDC)      │
│  WebSocket   │  KY-004 中断+OneButton       │
├──────────────┴────────────────────────────┤
│        开发框架: Arduino-ESP32 Core          │
├─────────────────────────────────────────────┤
│           硬件: ESP32-S3 (双核240MHz)         │
└─────────────────────────────────────────────┘
```

---

## 📦 依赖库清单（Arduino IDE 安装）

| 库名 | 用途 | 安装方式 |
|---|---|---|
| LVGL v8+ | UI框架，任务卡片/进度条/仪表盘 | Library Manager |
| TFT_eSPI（可选） | 轻量替代UI方案 | Library Manager |
| OneButton | 按键短按/长按/双击封装 | Library Manager |
| esp_now (内置) | 工牌间点对点通信 | Arduino-ESP32自带 |
| PubSubClient | MQTT客户端（连服务器） | Library Manager |
| ESP32 Arduino Core | 底层开发框架 | Board Manager |
| ArduinoJson | 数据序列化/通信报文 | Library Manager |
